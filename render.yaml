services:
  - type: web
    name: sleeper-mcp
    runtime: python
    buildCommand: "pip install uv && uv sync"
    startCommand: "uv run python app.py http"
    envVars:
      - key: RENDER
        value: "true"
      - key: PYTHON_VERSION
        value: "3.13"
    autoDeploy: true
    healthCheckPath: /health